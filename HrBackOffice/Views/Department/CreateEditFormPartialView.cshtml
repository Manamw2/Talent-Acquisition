@model Department

<div class="container-fluid px-4">
    <h1 class="mt-4 mb-4">@(Model.DepartmentId == 0 ? "Create New Department" : "Edit Department")</h1>
    <div class="row">
        <div class="col-xl-8">
            <div class="card mb-4">
                <div class="card-header">
                    <i class="fas fa-calendar-alt me-1"></i>
                    Department Information
                </div>
                <div class="card-body">
                    <form method="post">
                        @if (Model.DepartmentId > 0)
                        {
                            <input type="hidden" asp-for="DepartmentId" />
                        }
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                        <div class="mb-3">
                            <label asp-for="Name" class="small mb-1 fw-bold">Department Name</label>
                            <input asp-for="Name" class="form-control" placeholder="Enter Department name" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>
                        
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-1"></i> Back to List
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-1"></i>
                                @(Model.DepartmentId > 0 ? "Save Changes" : "Create Department")
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}